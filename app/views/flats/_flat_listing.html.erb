<div class="flat_listing">
  <div class="row">
    <% flats.each do |flat| %>
      <div class="col-sm-6 col-lg-4">
        <%= render "flats/flat_card", :flat => flat %>
      </div>
    <% end %>
  </div>
</div>